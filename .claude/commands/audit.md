Your goal is to audit any vulunerable dependecies.

Do the following:
1. Run `npm audit` to find vulunerable installed packages in this project.
2. Run `npm audit fix` to apply updates
3. Run tests and verify the updates didn't break anything.
